{
  "name": "my-vue-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "rm -rf dist && npm run build && cd dist && git init && git checkout -b main && git add -A && git commit -m 'deploy' && git push -f https://github.com/Fengli-ruomeng/CyberDivination.git main:gh-pages && cd .."
  },
  "dependencies": {
    "element-plus": "^2.9.4",
    "openai": "^4.84.0",
    "vue": "^3.3.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^4.5.0",
    "vite": "^5.0.0"
  }
}
